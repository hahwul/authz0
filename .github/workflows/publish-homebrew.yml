---
name: Homebrew tap Publish
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g. 2.0.0)"
        required: true
        type: string
  workflow_run:
    workflows: ["Build and Release Binaries"]
    types: [completed]
permissions:
  contents: read
jobs:
  publish-formula:
    name: Publish prebuilt formula to tap
    # Only run after a release-triggered binary build succeeds, or on manual
    # dispatch. The prebuilt formula needs the release binaries to exist first.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'release')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source (formula template)
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('v{0}', github.event.inputs.version) || github.event.workflow_run.head_branch }}

      - name: Resolve version
        id: ver
        env:
          EVENT_NAME: ${{ github.event_name }}
          DISPATCH_VERSION: ${{ github.event.inputs.version }}
          RUN_BRANCH: ${{ github.event.workflow_run.head_branch }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            RAW="$DISPATCH_VERSION"
          else
            RAW="$RUN_BRANCH"
          fi
          VERSION="${RAW#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $VERSION"

      - name: Download release binaries and compute checksums
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          V: ${{ steps.ver.outputs.version }}
        run: |
          for asset in osx-arm64 osx-x86_64 linux-arm64 linux-x86_64; do
            gh release download "v$V" --repo hahwul/authz0 \
              --pattern "authz0-v$V-$asset" --output "authz0-v$V-$asset"
          done
          {
            echo "SHA_OSX_ARM64=$(sha256sum "authz0-v$V-osx-arm64" | awk '{print $1}')"
            echo "SHA_OSX_X86_64=$(sha256sum "authz0-v$V-osx-x86_64" | awk '{print $1}')"
            echo "SHA_LINUX_ARM64=$(sha256sum "authz0-v$V-linux-arm64" | awk '{print $1}')"
            echo "SHA_LINUX_X86_64=$(sha256sum "authz0-v$V-linux-x86_64" | awk '{print $1}')"
          } >> "$GITHUB_ENV"

      - name: Render formula
        env:
          V: ${{ steps.ver.outputs.version }}
        run: |
          mkdir -p out
          sed \
            -e "s|__VERSION__|$V|g" \
            -e "s|__SHA_OSX_ARM64__|$SHA_OSX_ARM64|g" \
            -e "s|__SHA_OSX_X86_64__|$SHA_OSX_X86_64|g" \
            -e "s|__SHA_LINUX_ARM64__|$SHA_LINUX_ARM64|g" \
            -e "s|__SHA_LINUX_X86_64__|$SHA_LINUX_X86_64|g" \
            .github/homebrew/authz0.rb.tmpl > out/authz0.rb
          echo "----- rendered formula -----"
          cat out/authz0.rb

      - name: Checkout Homebrew tap
        uses: actions/checkout@v6
        with:
          repository: hahwul/homebrew-authz0
          token: ${{ secrets.AUTHZ0_PUBLISH_TOKEN }}
          path: tap

      - name: Commit and push formula
        env:
          V: ${{ steps.ver.outputs.version }}
        run: |
          mkdir -p tap/Formula
          cp out/authz0.rb tap/Formula/authz0.rb
          cd tap
          git config user.name "hahwul"
          git config user.email "hahwul@gmail.com"
          git add Formula/authz0.rb
          if git diff --cached --quiet; then
            echo "Formula unchanged; nothing to publish."
            exit 0
          fi
          git commit -m "authz0 $V"
          git push
